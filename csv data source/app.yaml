name: CSV data source
language: python
variables:
  - name: output
    inputType: OutputTopic
    multiline: false
    description: Name of the output topic to write into
    defaultValue: cnc-data
    required: true
dockerfile: dockerfile
runEntryPoint: main.py
defaultFile: main.py
